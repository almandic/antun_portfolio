spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/match_results
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org.springframework: DEBUG